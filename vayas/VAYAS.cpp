#include "VAYAS.h"

VAYAS::VAYAS(int c)
{
    clave = c;
}

string VAYAS::cifrar(string &messag)
{
    string cifrado;
    int pos = 0;
    for(int i = 0; i < clave; i++)
    {
        for(int j = 0; j < messag.size()/clave; j++)
        {

        }
    }

}
